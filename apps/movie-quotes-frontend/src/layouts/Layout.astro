---
import './Layout.css';

export interface Props {
  title: string;
  page?: string;
}

const { title, page } = Astro.props;

const navActiveClasses = "font-bold bg-yellow-400 no-underline";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
  </head>
  <body>
    <!-- <header class="prose mx-auto mb-6"> -->
    <header>
      <h1>🎬 Movie Quotes</h1>
    <!-- </header> -->
    <!-- <nav class="prose mx-auto mb-6 border-y border-gray-200 flex"> -->
    <nav>
      <a href="/?sort=createdAt" class={`${page === "listing-createdAt" && navActiveClasses}`}>Latest quotes</a>
      <a href="/?sort=likes" class={`${page === "listing-likes" && navActiveClasses}`}>Top quotes</a>
      <a href="/add" class={`${page === "add" && navActiveClasses}`}>Add a quote</a>
    </nav>
  </header>

    <!-- <section class="prose mx-auto"> -->
    <section>
      <slot />
    </section>
  </body>
</html>
